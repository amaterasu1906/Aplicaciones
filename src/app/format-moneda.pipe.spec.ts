import { FormatMonedaPipe } from './format-moneda.pipe';

describe('FormatMonedaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatMonedaPipe();
    expect(pipe).toBeTruthy();
  });
});
